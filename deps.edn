{:paths ["src" "resources"]
 ;; connector is needed only by github.connector, which exposes this client as
 ;; tools with an OAuth profile. github.workflow and github.main do not use it.
 :deps {org.clojure/clojure {:mvn/version "1.11.1"}
        io.github.kotoba-lang/connector
        {:git/url "https://github.com/kotoba-lang/connector"
         :git/sha "10a30f6dcb71be24a93ae7e434d62423396b7c7c"}}
 :aliases
 {:harness {:main-opts ["-m" "github.workflow.harness"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; clj-kondo as a Clojars lib (no Homebrew/system install needed).
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}
